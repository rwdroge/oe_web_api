---
trigger: always_on
---

#abl is case-insensitive by default 
- ABL string comparisons are case-insensitive by default. Functions like LOWER() or UPPER() are not necessary when comparing strings for equality if case does not matter.
