/*------------------------------------------------------------------------
    File        : MockCustomerRepository
    Purpose     : Mock repository for CustomerService testing
    Syntax      :
    Description : Provides mock data and methods for testing CustomerService
    Author(s)   : Generated by Kilo Code
    Created     :
    Notes       : Implements IRepository for customer-specific mock methods
  ----------------------------------------------------------------------*/

using Progress.Lang.*.
using tests.services.MockRepository.
using Progress.Json.ObjectModel.*.
using interfaces.IRepository.
using generics.FilterParams.

block-level on error undo, throw.

class tests.mocks.MockCustomerRepository implements IRepository:

    /* Property to control empty dataset returns */
    define public property ReturnEmptyDataset as logical no-undo get. set.

    /* Constructor */
    constructor public MockCustomerRepository():
    end constructor.

    /* Mock implementation of FindCustomersByCountry */
    method public handle FindCustomersByCountry(input pcCountry as character):
        var handle hDataSet.
        var handle hBuffer.

        /* Create mock dataset */
        create dataset hDataSet.
        create temp-table hBuffer.

        /* Add fields to temp-table */
        hBuffer:add-new-field("custnum", "integer").
        hBuffer:add-new-field("name", "character").
        hBuffer:add-new-field("country", "character").
        hBuffer:add-new-field("creditlimit", "decimal").

        /* Prepare temp-table */
        hBuffer:temp-table-prepare("ttCustomer").
        hDataSet:add-buffer(hBuffer).

        /* Add mock data if not returning empty */
        if not this-object:ReturnEmptyDataset then do:
            hBuffer:buffer-create().
            hBuffer::custnum = 1001.
            hBuffer::name = "Mock Customer 1".
            hBuffer::country = pcCountry.
            hBuffer::creditlimit = 5000.00.

            hBuffer:buffer-create().
            hBuffer::custnum = 1002.
            hBuffer::name = "Mock Customer 2".
            hBuffer::country = pcCountry.
            hBuffer::creditlimit = 7500.00.
        end.

        return hDataSet.

        finally:
            /* Cleanup will be handled by caller */
        end finally.
    end method.

    /* Mock implementation of FindCustomersBySalesRep */
    method public handle FindCustomersBySalesRep(input pcSalesRep as character):
        var handle hDataSet.
        var handle hBuffer.

        /* Create mock dataset */
        create dataset hDataSet.
        create temp-table hBuffer.

        /* Add fields to temp-table */
        hBuffer:add-new-field("custnum", "integer").
        hBuffer:add-new-field("name", "character").
        hBuffer:add-new-field("salesrep", "character").
        hBuffer:add-new-field("creditlimit", "decimal").

        /* Prepare temp-table */
        hBuffer:temp-table-prepare("ttCustomer").
        hDataSet:add-buffer(hBuffer).

        /* Add mock data if not returning empty */
        if not this-object:ReturnEmptyDataset then do:
            hBuffer:buffer-create().
            hBuffer::custnum = 1001.
            hBuffer::name = "Mock Customer 1".
            hBuffer::salesrep = pcSalesRep.
            hBuffer::creditlimit = 5000.00.

            hBuffer:buffer-create().
            hBuffer::custnum = 1002.
            hBuffer::name = "Mock Customer 2".
            hBuffer::salesrep = pcSalesRep.
            hBuffer::creditlimit = 7500.00.
        end.

        return hDataSet.

        finally:
            /* Cleanup will be handled by caller */
        end finally.
    end method.

    /* Mock implementations of IRepository methods */
    method public handle FindById(input pcId as character):
        var handle hDataSet.
        var handle hBuffer.
        var integer iCustNum = 0.

        /* Validate and convert ID */
        do on error undo, throw:
            iCustNum = integer(pcId).
            catch e as Progress.Lang.Error:
                /* Return empty dataset for invalid ID */
                create dataset hDataSet.
                return hDataSet.
            end.
        end.

        /* Create mock dataset */
        create dataset hDataSet.
        create temp-table hBuffer.

        /* Add fields to temp-table */
        hBuffer:add-new-field("custnum", "integer").
        hBuffer:add-new-field("name", "character").
        hBuffer:add-new-field("country", "character").
        hBuffer:add-new-field("creditlimit", "decimal").

        /* Prepare temp-table */
        hBuffer:temp-table-prepare("ttCustomer").
        hDataSet:add-buffer(hBuffer).

        /* Add mock data */
        hBuffer:buffer-create().
        hBuffer::custnum = iCustNum.
        hBuffer::name = "Mock Customer " + pcId.
        hBuffer::country = "USA".
        hBuffer::creditlimit = 5000.00.

        return hDataSet.
    end method.

    method public handle FindByFilter(input pFilter as generics.FilterParams):
        var handle hDataSet.
        var handle hBuffer.

        /* Create mock dataset */
        create dataset hDataSet.
        create temp-table hBuffer.

        /* Add fields to temp-table */
        hBuffer:add-new-field("custnum", "integer").
        hBuffer:add-new-field("name", "character").
        hBuffer:add-new-field("country", "character").
        hBuffer:add-new-field("creditlimit", "decimal").

        /* Prepare temp-table */
        hBuffer:temp-table-prepare("ttCustomer").
        hDataSet:add-buffer(hBuffer).

        /* Add mock data if not returning empty */
        if not this-object:ReturnEmptyDataset then do:
            hBuffer:buffer-create().
            hBuffer::custnum = 1001.
            hBuffer::name = "Mock Customer 1".
            hBuffer::country = "USA".
            hBuffer::creditlimit = 5000.00.

            hBuffer:buffer-create().
            hBuffer::custnum = 1002.
            hBuffer::name = "Mock Customer 2".
            hBuffer::country = "USA".
            hBuffer::creditlimit = 7500.00.
        end.

        return hDataSet.
    end method.

    method public handle Save(input poEntity as JsonObject):
        var handle hDataSet.
        var handle hBuffer.
        var character cName = "".
        var integer iCustNum = 9999.

        /* Extract data from input */
        if poEntity:Has("name") then
            cName = poEntity:GetCharacter("name").
        else
            cName = "New Customer".

        if poEntity:Has("custnum") then
            iCustNum = poEntity:GetInteger("custnum").

        /* Create mock dataset */
        create dataset hDataSet.
        create temp-table hBuffer.

        /* Add fields to temp-table */
        hBuffer:add-new-field("custnum", "integer").
        hBuffer:add-new-field("name", "character").
        hBuffer:add-new-field("country", "character").
        hBuffer:add-new-field("creditlimit", "decimal").

        /* Prepare temp-table */
        hBuffer:temp-table-prepare("ttCustomer").
        hDataSet:add-buffer(hBuffer).

        /* Add created customer data */
        hBuffer:buffer-create().
        hBuffer::custnum = iCustNum.
        hBuffer::name = cName.
        hBuffer::country = "USA".
        hBuffer::creditlimit = 0.00.

        return hDataSet.
    end method.

    method public handle UpdateEntity(input pcId as character, input poData as JsonObject):
        var handle hDataSet.
        var handle hBuffer.
        var character cName = "".
        var integer iCustNum = 0.
        var decimal dCreditLimit = 5000.00.

        /* Validate and convert ID */
        do on error undo, throw:
            iCustNum = integer(pcId).
            catch e as Progress.Lang.Error:
                /* Return empty dataset for invalid ID */
                create dataset hDataSet.
                return hDataSet.
            end.
        end.

        /* Extract data from input */
        if poData:Has("name") then
            cName = poData:GetCharacter("name").
        else
            cName = "Updated Customer".

        if poData:Has("creditlimit") then
            dCreditLimit = poData:GetDecimal("creditlimit").

        /* Create mock dataset */
        create dataset hDataSet.
        create temp-table hBuffer.

        /* Add fields to temp-table */
        hBuffer:add-new-field("custnum", "integer").
        hBuffer:add-new-field("name", "character").
        hBuffer:add-new-field("country", "character").
        hBuffer:add-new-field("creditlimit", "decimal").

        /* Prepare temp-table */
        hBuffer:temp-table-prepare("ttCustomer").
        hDataSet:add-buffer(hBuffer).

        /* Add updated customer data */
        hBuffer:buffer-create().
        hBuffer::custnum = iCustNum.
        hBuffer::name = cName.
        hBuffer::country = "USA".
        hBuffer::creditlimit = dCreditLimit.

        return hDataSet.
    end method.

    method public logical Delete(input pcId as character):
        /* Mock successful deletion */
        return true.
    end method.

    method public longchar GetMetadata():
        var longchar lcMetadata.
        
        lcMetadata = '~{"entity"~:"customer","fields"~:[~{"name"~:"custnum","type"~:"integer","required"~:true~},~{"name"~:"name","type"~:"character","required"~:true~},~{"name"~:"country","type"~:"character","required"~:false~},~{"name"~:"creditlimit","type"~:"decimal","required"~:false~}]~}'.
        
        return lcMetadata.
    end method.

end class.